# PaddleOCR 本地服务依赖
# 安装前请先确认 Python 版本（推荐 3.8–3.11）
# Windows 下请参考 README.md 中的详细安装说明

fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20
Pillow>=9.0.0
numpy>=1.21.0

# PaddlePaddle CPU 版（Windows）
# 注意：请根据实际 Python 版本和系统选择正确的版本
# Python 3.8–3.11 + Windows CPU：
paddlepaddle==2.6.2

# PaddleOCR
paddleocr==2.7.3

# 图像处理
opencv-python-headless>=4.6.0

# PDF 支持（可选，安装后支持 PDF 上传）
# PyMuPDF 自带 libmupdf，无需额外安装 Poppler
PyMuPDF>=1.23.0

# shapely 依赖（PaddleOCR 内部使用）
shapely>=1.7.0
